trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  displayName: 'Build Stage'
  jobs:
  - job: BuildJob
    displayName: 'Compilação e Validação'
    steps:
    - script: echo "Pipeline conectado com sucesso ao GitHub!"
      displayName: 'Mensagem de Teste'